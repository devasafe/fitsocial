node_modules/
dist/
build/
.env
.env.local
*.log
npm-debug.log*
.DS_Store
coverage/

# Uploads de imagens (armazenamento local de dev)
api/uploads/

# Expo
app-android/.expo/
app-android/web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

.vercel

# Chave de serviço do Firebase: credencial de ADMINISTRADOR. Quem a tem manda
# push em nome do app. Nunca entra no repositório.
app-android/credenciais/

# O `google-services.json` NÃO está aqui, de propósito. Ele só identifica o
# projeto — a autorização vem das regras do Firebase, não dele — e viaja dentro
# de todo APK distribuído, então guardá-lo seria encenação. Além disso, o EAS
# Build só envia o que está versionado: ignorá-lo quebra o build.
